// Explicit coercion is when you convert a value from one type to another. This is done using built-in functions like Number(), String(), Boolean(), or using operators like + to concatenate strings.

const num = 5;

const str = String(num); // '5'

const stringToNumber = Number(str); // 5

const bool = Boolean(num); // true
